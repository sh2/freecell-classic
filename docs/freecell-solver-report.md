# フリーセルソルバー実装レポート

> 対応状況: 2026-08-18 に更新。`npm test`(単体 139 / E2E 62)が
> すべて成功。変更の要約は [CHANGELOG.md](../CHANGELOG.md) を参照。

## 概要

- Microsoft FreeCell 互換のフリーセル(クラシックルール)に対して、
  現在の盤面から「勝ち手順(移動のリスト)」を探索するソルバーを追加した。
- 実装形態はユーザー要件に基づく 3 点:
  1. ブラウザ組み込み(ツールバーの「ヒント」「自動解答」ボタン)。
  2. 出力は勝ち手順の列(移動のリスト)。
  3. ビットボード等の最適化を意識した高速寄りの実装。
- 探索は Web Worker 上で実行し、画面をブロックしない。

## 目的と要件

- 現状の盤面からゲームを解き、クリアまでの手順を提示する。
- 「ヒント」は手順を表示するだけ(盤面を進めない)。
- 「自動解答」は手順を 1 手ずつ再生してクリアまで進める。
- 探索は必ず終了する(ノード数・時間の上限で打ち切る)。
- 探索コアは DOM に依存しない純粋関数とし、単体テスト可能にする。

## 設計

### モジュール構成

| ファイル | 責務 |
| --- | --- |
| `src/js/solver.js` | 探索エンジン(純粋関数 `solve`) |
| `src/js/solver.worker.js` | `solve` を Worker スレッドで実行 |
| `src/js/solver-client.js` | Worker の管理・結果表示・手順再生 |
| `src/js/view.js` | 解答パネル・ボタン無効化などの DOM |
| `src/js/app.js` | 再生用の `applyMoveInstant` を追加 |
| `src/js/main.js` | ボタン配線・テスト API 公開 |
| `index.html` / `src/css/style.css` | ボタンと解答パネルの UI |

### 盤面の表現

- カードは id(0〜51)で表す。既存のディールと同じ規則:
  - `suit = id % 4`(0=♣, 1=♦, 2=♥, 3=♠)
  - `rank = Math.floor(id / 4) + 1`(ビット演算では `id >> 2` と `id & 3`)
  - 赤は `suit === 1 || suit === 2`
- 探索中の状態は 1 つの可変状態を使い回す(make/unmake 方式):
  - `cols`: 8 列。各列はカード id の配列(最大長 19 を想定)。
  - `free`: `Int8Array(4)`。空きは `-1`。
  - `found`: `Int8Array(4)`。各スートの土台先頭ランク(0=空)。
  - `totalHome` / `totalCards`: 勝利判定とヒューリスティック用の枚数。
    `totalCards` を動的に数えるため、52 枚に満たない部分盤面も解ける。

## アルゴリズム

### 全体: IDA\*(反復深化 A\*)

- 反復深化 DFS にヒューリスティック下限 `h` を組み合わせ、
  `f = g + h` が閾値を超えた枝を刈りながら、閾値を徐々に上げて再探索する。
- メモリは「現在の経路」+「置換表」だけで済み、A\* のような
  オープンリストの爆発がない。

### 自動ホーム(土台への移動の扱い)

- 土台への移動は戻せないため、2以上のカードを常に自動移動すると解を失う
  可能性がある。fastでは合法なホーム移動を無条件に自動適用し、safeでは
  `isSafeFoundationMove()` を満たす移動だけを自動適用する。安全条件を満たさない
  合法手は探索分岐として保持する。
- これにより状態空間が大幅に削減され、探索の各ノードは常に
  「ホームへ送れるカードが 1 枚もない正規化済み状態」になる。

### 手の生成

- ホームへの移動を除いた分岐手のみを列挙する:
  1. 列 → 列(スーパームーブ含む)。各列について、先頭から連続する
     降順・色交互の連続列(末尾)をすべて試し、置ける列へ移動する。
     移動可能枚数は既存ルールと同じ `(空きフリーセル+1) * 2^空き列`。
  2. 列 → フリーセル。
  3. フリーセル → 列。
- フリーセル → フリーセルは無意味なシャッフルなので生成しない。

### ヒューリスティック

```text
h = (残りカード数)
  + Σ 各列の「可動末尾より下にあるカード数」
```

- 残りカード数は、どの非ホームカードも最低 1 回は土台へ動かす必要がある
  という下界。
- 「可動末尾」とは、列の先頭からスーパームーブでまとめて動かせる連続列。
  その下のカードは上を退けるまで動かせないため 1 手分を加算する。
- fastでは後半の加算が過大評価になり得るため、探索順序を優先した境界として
  使用する。safeではこの加算を枝刈りの下限に使わず、残りカード数だけの
  証明可能な下限を使用する。いずれも最短解を保証しない。

### 置換表と Zobrist ハッシュ

- 同一状態の再探索を防ぐため、64bit(32bit × 2)の Zobrist ハッシュで
  状態を識別する置換表を用いる。
  - 鍵は `列(col, pos, card)`・`土台(suit, rank)`・`フリーセル(card)`
    に乱数を割り当て、make/unmake 時に XOR で増分更新する。
  - フリーセルは「どの空きマスか」を区別せず、置かれたカードだけで
    ハッシュする(フリーセルは対称なので正規化になる)。
- 置換表はオープンアドレス法 + 線形探索(容量 2^22 スロット)。
  同じ鍵には最小の `g` を保持し、`g` が既知値以上なら枝を刈る。64bit Zobrist
  ハッシュの衝突は実用上無視する方針であり、`unsolvable` は衝突が発生していない
  という仮定付きの結果で、数学的な完全証明ではない。

### 枝刈りと探索打ち切り

- fastでは直前の分岐手の「逆手」を生成せず、無意味な往復を抑制する。safeでは
  この枝刈りの安全性が未検証のため無効化する。
- 置換表の最小 `g` 枝刈りは「同一イテレーション内でのみ」正しいため、
  IDA\* の閾値を上げるたびに置換表をクリアする。これを怠ると、低い閾値で
  浅く刈った状態が高い閾値でも再展開されず、解を取りこぼす。
- `maxNodes` / `maxTimeMs`で打ち切り、`node-limit` / `time-limit` を返す。
  探索空間を走査し終えた場合は、64bit Zobrist衝突を無視した結果として
  `unsolvable` を返す。これは数学的な完全証明ではない。打ち切りは例外で探索を
  即座に脱出する。
- 勝利を検出した時点で `path` の複製を `solutionMoves` に保存する。
  (DFS の巻き戻しで `path` が pop されるため、後から参照すると壊れる)

## UI / Worker 統合

- `solver-client.js` が Worker の生成・破棄を管理し、結果受信後に
  `view.showSolution()` で解答手順パネルを表示する。
- 「自動解答」は受信後に `app.applyMoveInstant()` で 1 手ずつ再生する。
  自動ホームは順序を保つため一時的に無効化し、終了後に戻す。
- 計算中はボタンを無効化し「計算中…」と表示する。
- 新しいゲームを開始したら古い解答パネルを閉じる。

## テスト

### 単体テスト(`tests/unit/solver.test.js`)

- 勝利済み盤面(0 手)、あと 1 手の盤面(1 手)、A 4 枚(4 手)、
  退避が必要な盤面(7 手)、解けない盤面(上限到達)、決定的再現性、
  `cardName` / `formatMove` などを検証。
- 各テストは、得られた手順をゲーム本体の `attemptMove` で再生し、
  実際に全カードがホームへ揃うことまで確認する。

### E2E テスト(`tests/e2e/freecell.spec.js`)

- 「ヒント」で解答手順パネルが表示され、盤面が進まないこと。
- 「自動解答」で勝利オーバーレイが表示され、`won` になること。
- 解答パネルが閉じるボタンで閉じること。

## 性能評価

手元環境での参考値(Worker ではなく `solver.js` を直接呼んだ測定)。

| 対象 | 結果 | ノード数 | 時間 | 備考 |
| --- | --- | --- | --- | --- |
| Game #1 | solved(96 手) | 約 13,554 | 約 40ms | 手順を再生して勝利確認済み |
| Game #12 | node-limit | 2,000,000 | 約 1.8s | 難しめの配布 |
| Game #11982 | node-limit | 2,000,000 | 約 1.5s | 既知の「解なし」配布 |

- Game #1 は手順 96 手をゲーム本体の `attemptMove` で再生し、
  `isWon()` が true になることを確認した(探索と既存ルールの整合性を検証)。
- 解けない配布(#11982)は誤って「解けた」とはせず、上限到達として
  正しく打ち切る。

## 制限と今後の課題

- 現在のヒューリスティックでは、簡単〜中程度の配布は解けるが、
  難しい配布(例: Game #12)は既定上限内で解けない。
- 全 32,000 配布を安定して解くには以下が必要:
  - より強いヒューリスティック(Deadlock 検出、空き列の価値評価など)。
  - 列の対称性の正規化(置換表ヒット率の向上)。
  - より大きなノード/時間上限、または反復ごとの改善。
- 「解けない」ことの完全な証明は行わない。あくまで上限到達として報告する。

## 変更ファイル

- `src/js/solver.js` — 探索エンジン(新規)。
- `src/js/solver.worker.js` — Worker ラッパー(新規)。
- `src/js/solver-client.js` — UI クライアント(新規)。
- `src/js/view.js` — `setSolverBusy` / `showSolution` / `hideSolution` 等。
- `src/js/app.js` — `applyMoveInstant` を追加・公開。
- `src/js/main.js` — ボタン配線とテスト API 追加。
- `index.html` — 「ヒント」「自動解答」ボタンと解答手順パネル。
- `src/css/style.css` — 解答手順パネルのスタイル。
- `tests/unit/solver.test.js` — 単体テスト(新規)。
- `tests/e2e/freecell.spec.js` — E2E テスト追加。

## 9. 列正規化修正後の再ベンチマーク (2026-08-17)

### 9.1 検証条件

初回ベンチマーク(`batch-01.json`)で `node-limit` となった 134 ゲームだけを対象に、
列正規化キーの修正後アルゴリズムで再計測した。

| 項目 | 値 |
| --- | ---: |
| 対象 | Game #24 など、初回 `node-limit` の 134 ゲーム |
| `maxNodes` | 2,000,000 |
| `maxTimeMs` | 60,000 |
| 結果ファイル | `docs/benchmark/data/verify-node-limit-01-2000000.json` |

### 9.2 結果

| 状態 | 初回 | 再計測 |
| --- | ---: | ---: |
| solved | 0 | **134** |
| node-limit | 134 | **0** |
| time-limit | 0 | 0 |
| unsolvable | 0 | 0 |

再計測対象の 134 ゲームはすべて解決した。初回から継続して解決できていた 866
ゲームと合わせると、Game #1〜#1000 は **1,000 / 1,000 (100%)** が解決可能になった。

### 9.3 再計測対象の性能分布

| 指標 | 値 |
| --- | ---: |
| 総計算時間 | 23,432 ms |
| 1ゲームあたり平均時間 | 約 215 ms |
| 中央時間 | 160 ms |
| 最長時間 | 1,093 ms |
| ノード数の平均 | 約 186,257 |
| ノード数の中央値 | 131,947 |
| 最大ノード数 | 1,090,281 |
| 50万ノード未満 | 124 / 134 |
| 100万ノード超 | 2 / 134 |
| 解答手数 | 86〜96手 (平均 約93.2手) |

初回の 134 ゲームは全て 200 万ノードで打ち切られていたため、列正規化修正後は
同じノード上限で全件を解決できた。これは列の境界を保持した正規化により、誤った
状態同一視を避けつつ、探索の重複を大幅に削減できた結果と考えられる。

## 10. バッチ1の再計測 (2026-08-17)

### 10.1 データのバックアップ

再計測前に、従来の `docs/benchmark/data/` を次のディレクトリへ移動した。

```text
docs/benchmark/data-backup-20260817/
```

新しい結果は従来どおり `docs/benchmark/data/` に保存した。バックアップには、
初回の `batch-01.json`、5百万ノード再検証結果、既存の `report.html` などを含む。

### 10.2 検証条件

| 項目 | 値 |
| --- | ---: |
| 対象 | Game #1〜#1000 |
| `maxNodes` | 2,000,000 |
| `maxTimeMs` | 60,000 |
| 実行 | `node scripts/benchmark/run.js --batch 1` |
| HTMLレポート | `docs/benchmark/report.html` |

### 10.3 結果

| 状態 | 初回 | 再計測 |
| --- | ---: | ---: |
| solved | 866 | **1,000** |
| node-limit | 134 | **0** |
| time-limit | 0 | 0 |
| unsolvable | 0 | 0 |

更新後のアルゴリズムでは、Game #1〜#1000 の **1,000 / 1,000 (100%)** を解決できた。

### 10.4 性能分布

| 指標 | 再計測結果 |
| --- | ---: |
| 総計算時間 | 45,857 ms |
| 1ゲームあたり平均時間 | 約 54 ms |
| 中央時間 | 18 ms |
| 最長時間 | 1,338 ms |
| ノード数の平均 | 38,824 |
| ノード数の中央値 | 6,460 |
| 最大ノード数 | 1,269,995 |
| 10万ノード未満 | 900 / 1,000 |
| 50万ノード未満 | 989 / 1,000 |
| 100万ノード超 | 3 / 1,000 |
| 解答手数 | 76〜96手 (平均 93.0手) |

## 11. performance.now() 変更後のバッチ1再計測 (2026-08-17)

### 11.1 変更内容

VirtualBox Guest Additions などによるシステム時刻の補正が処理時間測定へ影響しない
よう、`src/js/solver.js` の時間計測を `Date.now()` から単調増加する
`performance.now()` へ変更した。時間上限判定と返却値 `timeMs` の両方に同じタイマーを
使用している。

再計測前の結果は `docs/benchmark/data-backup-20260817-performance-now/` へ移動し、
今回の結果を `docs/benchmark/data/` に保存した。

### 11.2 検証条件と結果

| 項目 | 値 |
| --- | ---: |
| 対象 | Game #1〜#1000 |
| `maxNodes` | 2,000,000 |
| `maxTimeMs` | 60,000 |
| `solved` | **1,000 / 1,000** |
| `node-limit` | 0 |
| `time-limit` | 0 |
| `unsolvable` | 0 |
| 総計算時間 | 54,006 ms |

### 11.3 時間・ノード数の統計

| 指標 | 結果 |
| --- | ---: |
| 1ゲームあたり平均時間 | 約54.0 ms |
| 中央時間 | 17 ms |
| 最短時間 | 6 ms |
| 最長時間 | 1,367 ms |
| 負の `timeMs` | **0件** |
| 500 ms超 | 13件 |
| 1秒超 | 3件 |
| ノード数の平均 | 38,824 |
| ノード数の中央値 | 6,460 |
| 最大ノード数 | 1,269,995 |

`performance.now()` 変更後は、全1,000件で `timeMs` が負にならず、VirtualBox環境で
発生していた時刻補正による測定値の逆行を解消できた。

## 12. 自動ホーム移動の安全条件 (2026-08-17)

### 12.1 調査結果

ホームへ置けるカードを直ちに送ると、そのカードを tableau の受け皿として使えなく
なる。特に 2 以上のカードでは、反対色スートの低いカードを一時的に重ねるために
必要になる可能性がある。そのため「合法なら常に安全」とは限らない。

### 12.2 実装した条件

Horne's Rule に基づき、次の条件で自動ホーム移動を行う。

- A は常に安全として自動移動する。
- rank $r$ のカードは、反対色 2 スートの土台がともに少なくとも $r-1$ まで
  進んでいる場合だけ自動移動する。
- 合法だが安全条件を満たさないホーム移動は、探索手として残す。

この構成では、安全なホーム移動による状態正規化の利点を維持しながら、安全でない
ホーム移動による解の取りこぼしを防ぐ。安全でないホーム移動は自動処理より低い優先度
で試行する。

### 12.3 検証

`isSafeFoundationMove()` の単体テストを追加し、次を確認した。

- A は常に安全
- 2 は反対色の A が揃うまで安全でない
- 3 は反対色の 2 が揃うまで安全でない
- 安全条件により手順が増える小盤面でも、手順を再生して勝利できる

単体テスト 139 件および E2E テストは成功した。

## 13. 安全条件導入後のバッチ1再計測 (2026-08-17)

### 13.1 検証条件

安全条件導入前の結果を `docs/benchmark/data-backup-20260817-safe-home/` へ退避し、
安全条件導入後の結果を `docs/benchmark/data/` へ保存した。

| 項目 | 値 |
| --- | ---: |
| 対象 | Game #1〜#1000 |
| `maxNodes` | 2,000,000 |
| `maxTimeMs` | 60,000 |
| 結果ファイル | `docs/benchmark/data/batch-01.json` |
| HTMLレポート | `docs/benchmark/report.html` |

### 13.2 解決率の比較

| 状態 | 安全条件導入前 | 安全条件導入後 | 差分 |
| --- | ---: | ---: | ---: |
| solved | **1,000** | 992 | **-8** |
| node-limit | 0 | 8 | **+8** |
| time-limit | 0 | 0 | 0 |
| unsolvable | 0 | 0 | 0 |

安全条件導入後は、解決率が 1,000 / 1,000 (100%) から 992 / 1,000 (99.2%) に低下した。
未解決になったゲームは #37、#49、#229、#336、#382、#456、#585、#853 で、いずれも
2百万ノード上限に到達した。

### 13.3 性能比較

| 指標 | 導入前 | 導入後 | 変化 |
| --- | ---: | ---: | ---: |
| 総計算時間 | 54,006 ms | 112,963 ms | 約2.09倍 |
| 平均時間 | 54.0 ms | 113.0 ms | 約2.09倍 |
| 中央時間 | 17 ms | 30 ms | 約1.76倍 |
| 平均ノード数 | 38,824 | 91,061 | 約2.35倍 |
| ノード数中央値 | 6,460 | 16,921 | 約2.62倍 |
| 最大ノード数 | 1,269,995 | 2,000,000 | 上限到達 |
| 負の `timeMs` | 0 | 0 | 維持 |

安全条件導入後は、217ゲームでノード数が減少した一方、783ゲームで増加した。
全体では探索分岐として残した安全でないホーム移動の影響が大きく、解決数・平均ノード数・
処理時間のいずれも悪化した。今回の実装は安全性の向上という目的には沿うが、現在の
move ordering のままでは探索効率を改善する結果にはならなかった。

### 13.4 解釈と次の方針

今回の結果から、Horne's Rule を導入した状態で全1,000件の解決率を維持するには、
安全でないホーム移動の探索順序を改善する必要がある。特に、ホーム移動を単純に低い
固定スコアで後回しにするのではなく、移動によって解放されるカード、空き列、フリーセル、
反対色スートの進行を評価する move ordering が必要である。

現時点では、導入前の無条件自動ホームの方がベンチマーク上の解決率と速度に優れる。
安全条件版は直ちに既定方式へ置き換えず、次の探索順序改善と組み合わせて再評価する。

## 14. 実験1: 安全でないホーム移動の優先度向上 (2026-08-17)

### 14.1 実験内容

安全でない合法ホーム移動のスコアを `1,000` から `12,000` へ変更した。
これにより、安全でないホーム移動を通常の単独列移動より優先し、空列を作る移動など
より高い評価の手は従来どおり先に試す構成とした。

比較対象の結果は `docs/benchmark/data-backup-20260817-home-priority-12000/` へ退避し、
実験結果は `docs/benchmark/data/` に保存した。

### 14.2 検証条件

| 項目 | 値 |
| --- | ---: |
| 対象 | Game #1〜#1000 |
| `maxNodes` | 2,000,000 |
| `maxTimeMs` | 60,000 |
| 安全でないホーム移動のスコア | `12,000` |
| 結果ファイル | `docs/benchmark/data/batch-01.json` |

### 14.3 結果

| 指標 | 実験前 | 実験1 | 変化 |
| --- | ---: | ---: | ---: |
| solved | 992 | **995** | **+3** |
| node-limit | 8 | **5** | **-3** |
| 総計算時間 | 112,963 ms | **101,806 ms** | **-9.9%** |
| 平均時間 | 113.0 ms | **101.8 ms** | **-9.9%** |
| 中央時間 | 30 ms | 31 ms | +1 ms |
| 平均ノード数 | 91,061 | **77,532** | **-14.9%** |
| ノード数中央値 | 16,921 | **14,383** | **-15.0%** |
| 最大ノード数 | 2,000,000 | 2,000,000 | 維持 |

未解決ゲームは #37、#224、#229、#336、#456 の5件で、すべて `node-limit` だった。

### 14.4 評価

安全でないホーム移動の優先度を引き上げたことで、解決数は 992 件から 995 件へ増加し、
平均ノード数は約14.9%、総計算時間は約9.9%削減された。比較対象よりノード数が減少した
ゲームは 657 件、増加したゲームは 338 件だった。

一方、初回の無条件自動ホーム方式(1,000 / 1,000)にはまだ届いていない。また、中央値の
処理時間は 30 ms から 31 ms へわずかに増加している。したがって、実験1は安全条件版の
改善として有効だが、単独で最終採用するには追加の move ordering 改善が必要である。

## 15. 実験2〜4: ホーム移動の優先度評価拡張 (2026-08-17)

### 15.1 実験内容

実験1を基準に、次の3種類を順に評価した。各実験の直前結果は個別のバックアップ
ディレクトリへ退避した。

| 実験 | 安全でないホーム移動のスコア |
| --- | --- |
| 実験2 | 固定 `16,000` |
| 実験3 | `12,000 + rank × 500` |
| 実験4 | 実験3 + 列解放 `+10,000` / 次の安全なホーム移動露出 `+5,000` |

実験4の結果は `docs/benchmark/data/` に保存されている。

### 15.2 比較結果

| 指標 | 実験1 | 実験2 | 実験3 | 実験4 |
| --- | ---: | ---: | ---: | ---: |
| solved | 992 | 995 | **997** | 995 |
| node-limit | 8 | 5 | **3** | 5 |
| 総計算時間 | 112,963 ms | 101,806 ms | **92,942 ms** | 98,286 ms |
| 平均時間 | 113.0 ms | 101.8 ms | **92.9 ms** | 98.3 ms |
| 中央時間 | 30 ms | 31 ms | **28 ms** | 27 ms |
| 平均ノード数 | 91,061 | 77,532 | **71,589** | 76,713 |
| ノード数中央値 | 16,921 | 14,383 | **12,895** | 14,861 |

### 15.3 未解決ゲーム

| 実験 | 未解決ゲーム |
| --- | --- |
| 実験2 | #37, #224, #229, #336, #456 |
| 実験3 | #87, #229, #336 |
| 実験4 | #37, #224, #229, #336, #456 |

### 15.4 評価

今回の範囲では、**実験3が最良**だった。実験1と比較すると、解決数は 992 件から
997 件へ増加し、平均ノード数は約21.4%、総計算時間は約17.7%削減された。
安全でないホーム移動のランクに応じた優先度が、固定スコアより有効だったと考えられる。

実験4は、列解放や安全なホーム移動の露出を加点したが、実験3より解決数・平均ノード数・
総計算時間のいずれも悪化した。単純な露出加点は、盤面を改善する手を選べる一方で、
高ランクホーム移動の有効な順序を崩した可能性がある。

したがって、現時点の単一探索順序では実験3の
`12,000 + rank × 500` を次の候補として採用し、実験3で未解決の #87、#229、#336を
重点的に分析するのがよい。なお、無条件自動ホーム方式の 1,000 / 1,000 にはまだ
到達していないため、最終的な既定値変更は全体方針を確認してから行う。

## 16. 自動ホーム方式の探索カウンター比較 (2026-08-17)

### 16.1 計測方法

`src/js/solver.js` に探索カウンターを追加し、次の2方式を同一条件で計測した。

- **無条件自動ホーム**: `safeFoundationMoves: false`。合法なホーム移動をすべて自動適用
- **安全条件 + 実験3**: `safeFoundationMoves: true`。安全でないホーム移動は探索分岐とし、
  スコアは `12,000 + rank × 500`

ベンチマークスクリプトには `--unsafe-home` オプションを追加した。無条件自動ホームの
結果は `docs/benchmark/data-counters-unconditional/`、安全条件版の結果は
`docs/benchmark/data/` に保存した。計測条件は `maxNodes=2,000,000`、
`maxTimeMs=60,000`、Game #1〜#1000 である。

### 16.2 解決状況と性能

| 指標 | 無条件自動ホーム | 安全条件 + 実験3 |
| --- | ---: | ---: |
| solved | **1,000** | 995 |
| node-limit | 0 | 5 |
| 総計算時間 | **53,018 ms** | 100,120 ms |
| 中央時間 | **16 ms** | 28 ms |
| 平均ノード数 | **38,824** | 77,931 |
| 最大探索深度 | 96 | 96 |
| 行き止まりノード | 445,978 | 417,715 |
| 置換表ヒット | 5,203,636 | 12,326,465 |

安全条件版は無条件自動ホームと比べ、平均ノード数が約2.01倍、総計算時間が約1.89倍
になった。解決数も 1,000 件から 995 件へ減少した。

### 16.3 安全でないホーム移動のカウンター

| カウンター | 無条件自動ホーム | 安全条件 + 実験3 |
| --- | ---: | ---: |
| 安全でないホーム手の生成 | 0 | 4,648,809 |
| 安全でないホーム手の試行 | 0 | 4,624,216 |
| 安全でないホーム手を含む解決到達 | 0 | 12,239 |
| 安全でないホーム手からの `INF` 終了 | 0 | 3,727,430 |
| 安全でないホーム手を試行したゲーム数 | 0 | 1,000 |
| 安全でないホーム手を含む解決ゲーム数 | 0 | 994 |

安全条件版では、4,624,216回の安全でないホーム手が試行され、そのうち解決到達として
記録されたものは 12,239回だった。一方、3,727,430回は `INF` で終了した。ここでの
`INF` は純粋な行き止まりだけでなく、置換表による枝刈りを含むため、すべてを「悪い手に
よる行き止まり」と解釈してはいけない。ただし、安全でないホーム手が大量の追加探索を
発生させていることは確認できる。

安全条件版で安全でないホーム手を含む解決に到達したゲームは 994件あり、この分岐が
常に不要だったわけではない。しかし、無条件自動ホームは安全でないホーム分岐を一切
生成せず、Game #1〜#1000を全件解決している。今回の範囲では、無条件自動ホームが
解決率と計算量の両方で優れているという仮説を支持する結果になった。

## 17. 高速実用モードのバッチ2〜5検証 (2026-08-17)

### 17.1 検証条件

安全でないホーム移動も含め、合法なホーム移動を無条件に自動適用する高速実用モード
(`safeFoundationMoves: false`)で、バッチ2〜5を検証した。

| 項目 | 値 |
| --- | ---: |
| 対象 | Game #1001〜#5000 |
| `maxNodes` | 2,000,000 |
| `maxTimeMs` | 60,000 |
| 結果ディレクトリ | `docs/benchmark/data-counters-unconditional-batches-2-5/` |

### 17.2 バッチ別結果

| バッチ | ゲーム範囲 | solved | node-limit | time-limit | 総計算時間 |
| --- | --- | ---: | ---: | ---: | ---: |
| 2 | #1001〜#2000 | 999 | 1 | 0 | 60,877 ms |
| 3 | #2001〜#3000 | **1,000** | 0 | 0 | 51,408 ms |
| 4 | #3001〜#4000 | 997 | 3 | 0 | 66,051 ms |
| 5 | #4001〜#5000 | 996 | 4 | 0 | 60,952 ms |
| **合計** | **#1001〜#5000** | **3,992** | **8** | **0** | **239,288 ms** |

### 17.3 性能分布

| 指標 | 結果 |
| --- | ---: |
| 平均時間 | 約59.8 ms |
| 中央時間 | 19 ms |
| 平均ノード数 | 43,060 |
| 中央ノード数 | 6,658 |
| 最小ノード数 | 27 |
| 最大ノード数 | 2,000,000 |

未解決となったゲームは次の8件で、すべて `node-limit` だった。

`#1941, #3699, #3740, #3971, #4016, #4482, #4565, #4828`

### 17.4 Game #1〜#5000の累積結果

バッチ1の無条件自動ホーム結果(`1,000 / 1,000`)と今回のバッチ2〜5を合わせると、
Game #1〜#5000では次の結果になった。

| 指標 | 結果 |
| --- | ---: |
| solved | **4,992 / 5,000 (99.84%)** |
| node-limit | 8 |
| time-limit | 0 |
| unsolvable | 0 |
| 総計算時間 | 292,306 ms |

今回の検証では、無条件自動ホームが安全探索モードだけで解けるゲームを確認することは
できない。次の段階では、今回の8件を安全条件 + 実験3で同じノード上限で再計測し、
高速実用モードで未解決かつ安全探索モードで解決できるゲームが存在するかを調べる。

## 18. 高速実用モード未解決ゲームの安全探索 (2026-08-17)

### 18.1 検証条件

高速実用モードで `maxNodes=2,000,000` に到達した8ゲームを、安全条件 + 実験3の
安全探索モードで再計測した。

| 項目 | 値 |
| --- | ---: |
| 対象 | #1941、#3699、#3740、#3971、#4016、#4482、#4565、#4828 |
| `maxNodes` | 5,000,000 |
| `maxTimeMs` | 600,000 |
| `safeFoundationMoves` | `true` |
| 結果ディレクトリ | `docs/benchmark/data-safe-5000000-unresolved-01-5000/` |

### 18.2 結果

| ゲーム | 高速実用 200万 | 安全探索 500万 | ノード数 | 時間 | 手数 |
| ---: | --- | --- | ---: | ---: | ---: |
| #1941 | node-limit | solved | 265,145 | 444 ms | 93 |
| #3699 | node-limit | solved | 2,972,676 | 3,468 ms | 99 |
| #3740 | node-limit | solved | 2,216,209 | 2,648 ms | 98 |
| #3971 | node-limit | solved | 4,665,300 | 4,401 ms | 93 |
| #4016 | node-limit | **node-limit** | 5,000,000 | 4,427 ms | 0 |
| #4482 | node-limit | solved | 15,488 | 69 ms | 93 |
| #4565 | node-limit | solved | 2,696,947 | 2,768 ms | 94 |
| #4828 | node-limit | solved | 652,220 | 795 ms | 95 |

8ゲーム中 **7ゲームが安全探索モードで解決**し、#4016だけが500万ノードでも
`node-limit` となった。高速実用モードで未解決だったゲームのうち、安全探索モードで
追加解決できるゲームが実際に存在することを確認できた。

### 18.3 集計

| 指標 | 結果 |
| --- | ---: |
| solved | **7 / 8** |
| node-limit | 1 / 8 |
| time-limit | 0 |
| 総ノード数 | 18,483,985 |
| 平均ノード数 | 2,310,498 |
| 総計算時間 | 19,020 ms |
| 平均時間 | 2,378 ms |

この結果から、安全探索モードは探索コストを増やす一方で、高速実用モードでは未解決
だったゲームを追加で解決できる補完的な方式であることが分かった。今後は通常モードで
未解決になったゲームだけを安全探索モードへ切り替える二段階探索が有力な構成になる。

## 19. 未解決8ゲームの高速実用モード500万ノード検証 (2026-08-17)

### 19.1 検証条件

第17章で高速実用モードの `maxNodes=2,000,000` でも未解決だった8ゲームを、同じ
無条件自動ホーム方式のまま `maxNodes=5,000,000` で再計測した。

| 項目 | 値 |
| --- | ---: |
| 対象 | #1941、#3699、#3740、#3971、#4016、#4482、#4565、#4828 |
| `maxNodes` | 5,000,000 |
| `maxTimeMs` | 600,000 |
| `safeFoundationMoves` | `false` |
| 結果ディレクトリ | `docs/benchmark/data-unconditional-5000000-unresolved-01-5000/` |

### 19.2 結果比較

| ゲーム | 高速実用500万 | 安全探索500万 | 高速ノード数 | 安全ノード数 |
| ---: | --- | --- | ---: | ---: |
| #1941 | node-limit | solved | 5,000,000 | 265,145 |
| #3699 | solved | solved | 2,859,129 | 2,972,676 |
| #3740 | solved | solved | 2,032,091 | 2,216,209 |
| #3971 | solved | solved | 3,944,329 | 4,665,300 |
| #4016 | node-limit | node-limit | 5,000,000 | 5,000,000 |
| #4482 | solved | solved | 2,897,102 | 15,488 |
| #4565 | solved | solved | 2,104,420 | 2,696,947 |
| #4828 | solved | solved | 2,211,950 | 652,220 |

高速実用モードでは **6 / 8** ゲームを解決し、#1941 と #4016 は500万ノードでも
`node-limit` となった。安全探索モードは同じ8ゲームで7件を解決しており、#1941を
追加で解決できた。

### 19.3 集計比較

| 指標 | 高速実用500万 | 安全探索500万 |
| --- | ---: | ---: |
| solved | 6 / 8 | **7 / 8** |
| node-limit | 2 / 8 | 1 / 8 |
| 総ノード数 | 26,049,021 | **18,483,985** |
| 平均ノード数 | 3,256,128 | **2,310,498** |
| 総計算時間 | 26,994 ms | **19,020 ms** |
| 平均時間 | 3,374 ms | **2,378 ms** |

この8ゲームでは、安全探索モードの方が高速実用モードより1件多く解決し、総ノード数は
約29.1%、総計算時間は約29.5%少なかった。ただし、これは高速実用モードで既に深く
探索した難しいゲームだけを対象にした比較であり、全ゲームに対して安全探索モードが
高速であることを意味しない。通常は高速実用モードを先に実行し、`node-limit` のゲーム
だけ安全探索モードへ切り替える二段階方式が合理的である。

## 20. フェーズ4: 全32,000ゲーム評価 (2026-08-18)

最新実装で `fast-safe` 戦略を全32,000ゲームへ適用した。高速モードは2,000,000
ノード/30秒、安全モードは10,000,000ノード/180秒とし、安全モードでは逆手除外を
無効化した。64bit Zobristハッシュの衝突は実用上無視している。

| 指標 | 結果 |
| --- | ---: |
| 計測ゲーム数 | 32,000 |
| solved | 31,974 |
| 解決率 | 99.919% |
| node-limit | 26 |
| time-limit | 0 |
| unsolvable | 0 |
| 総ノード数 | 945,619,538 |
| 平均ノード数 | 29,550.6 |
| 総計算時間 | 1,326,484 ms |
| 平均計算時間 | 41.45 ms |
| 安全モードへフォールバック | 26ゲーム |

安全モードでも未解決だった26ゲームは、`#1941`、`#3670`、`#4016`、`#4828`、`#5495`、`#6240`、
`#9781`、`#10692`、`#11552`、`#11982`、`#12475`、`#13701`、`#13705`、`#13777`、`#14212`、`#17432`、
`#17880`、`#18783`、`#19975`、`#21491`、`#24327`、`#25315`、`#26334`、`#27101`、`#31433`、`#31465`で、
すべて `node-limit` だった。`time-limit` と `unsolvable` は発生しなかった。

`unsolvable` は64bit Zobrist衝突を無視した探索上の結果であり、数学的な完全証明では
ない。フェーズ3の網羅的な完全性検証は引き続き未完了である。
